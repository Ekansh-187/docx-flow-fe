export enum Scope {
	ConversionWrite = "conversion:write",
	ConversionRead = "conversion:read",
	DocumentRead = "document:read",
	DocumentWrite = "document:write",
	Admin = "admin",
}

export enum TokenName {
    FreeTier = "free-tier",
    PremiumTier = "premium-tier",
}